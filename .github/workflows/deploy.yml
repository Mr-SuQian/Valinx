name: Deploy Valinx Java Engine

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    name: 🚀 部署到腾讯云
    runs-on: ubuntu-latest

    steps:
      - name: 1. 拉取代码
        uses: actions/checkout@v4

      - name: 2. 设置 JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: 3. 编译打包 (ShadowJar)
        uses: gradle/gradle-build-action@v3
        with:
          gradle-version: 8.5
          arguments: shadowJar

      - name: 4. 压缩打包 (Optimize Speed)
        run: tar -czf release.tar.gz -C build/libs Valinx-Engine.jar

      - name: 5. 传输压缩包 (SCP)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "release.tar.gz"
          target: "/root/valinx/app"

      - name: 6. 远程解压与重启 (SSH)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /root/valinx/app
            tar -xzf release.tar.gz
            rm release.tar.gz
            
            cd /root/valinx
            # 停止旧进程
            fuser -k 3001/tcp || true 
            # 运行新版本
            nohup java -jar app/Valinx-Engine.jar > valinx.log 2>&1 &
            echo "Valinx Engine optimized & restarted."
